// You can edit this code!
// Click here and start typing.
package main

import "fmt"

//function
func getAvg(arr []float64) float64 {
	sum := 0.0
	for _, val := range arr {
		sum += val
	}
	return sum / float64(len(arr))
}

//method
//add a method to an object defined elsewhere in a file

/*
func (c *Cube) volume() float64 {
	return c.depth * c.width * c.height
}
*/

func main() {
	arr := []float64{1.0, 2.0, 3.0}
	fmt.Println(getAvg(arr))
}