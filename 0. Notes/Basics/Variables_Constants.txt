//this is a comment

// package names should be short and concised and should be suggestive
// package names are short single named words
package main

import (
	"fmt"
)

/*
primitives
	number
		integers
			int	8, 16, 32, 64
			uint	8, 16, 32, 64
		floating points
			float	32, 64
		complex(2 floats)
			complex 64, 128
	booleans
		true/false
	strings(sequnce of bytes)
		immutable

*/

const (
	Monday = iota
	Tuesday
	Wednesday
	Thursday
	Friday
	Saturday
	Sunday
)

func main() {
	//variable declaration
	var msg string = "a string"
	fmt.Println(msg)

	var msg2 string
	msg2 = "another string"
	fmt.Println(msg2)

	//inffer the type
	msg3 := "another one"
	fmt.Println(msg3)

	//for an int
	x := 1
	fmt.Println(x)

	//0 value for bool
	var truth bool
	fmt.Println(truth)

	//constants declaration
	//can't be changed
	//here we don't specify the type
	const pi = 3.1415
	fmt.Println(pi)
	fmt.Printf("type: %T, value: %v", pi, pi)

	//can't add floats to ints
	const myConst int = 3
	fmt.Println(myConst)
	//fmt.Printf("type: %T, value: %v", myConst+1.1, myConst+1.1)

	//using enum iota
	fmt.Println(Thursday)
}
